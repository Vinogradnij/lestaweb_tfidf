services:
  backend:
    build:
        dockerfile: ./Dockerfile
    container_name: backend_tfidf
    env_file:
      - path: .env-template
        required: true
      - path: .env
        required: false
    ports:
      - "8000:8000"
      - "${TFIDF__RUN__PORT}:${TFIDF__RUN__PORT}"